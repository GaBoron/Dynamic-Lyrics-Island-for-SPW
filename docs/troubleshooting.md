# 故障排查

[← 文档首页](README.md)

## 先试这几步

很多问题不用重装，先按下面的顺序排一下：

1. 确认下载的是当前平台对应的插件 ZIP，并且插件已经在 SPW 中启用；
2. 切换一次歌曲，必要时重启 SPW；
3. 歌词问题先确认 SPW 自己已经能显示歌词；
4. 找不到词岛时先点一次“找回词岛”；
5. 时序或实验功能异常时，先恢复到 1× 播放并暂时关闭实验性多行歌词。

拿一首本地歌曲、默认设置、1× 播放做对照，通常很快就能看出问题是在歌曲数据、歌词源、平台环境还是词岛本身。

## 安装与显示

### 插件没有出现在 SPW 插件列表

先确认：

- 下载的是 `windows-x64` / `linux-x64` 插件包，不是 `-source`；
- ZIP 没有被解压后又把里面的文件夹当插件装进去；
- SPW 版本兼容当前 Workshop API。

Windows 手动安装时，ZIP 应放在：

```text
%APPDATA%\Salt Player for Windows\workshop\plugins\
```

放入前先完全退出 SPW。详细步骤见 [安装与更新](installation.md)。

### 插件启用了，但词岛完全不显示

检查“显示词岛”“暂停时隐藏”，Windows 再检查“全屏时隐藏”。如果用过多显示器，也可能只是词岛还停在另一块屏幕的位置。

最直接的处理办法是：

**SPW 插件设置 → 找回词岛**

它会重新显示词岛并移回主屏幕顶部；Windows 下还会解除鼠标穿透。

`Ctrl+Shift+D` 也能显示 / 隐藏词岛，但目前只在 SPW 内生效。

### 词岛突然消失

常见原因有：

- 开了“暂停时隐藏”；
- Windows 开了“全屏时隐藏”；
- Windows 开了鼠标穿透和悬停自动隐藏；
- 显示器连接状态变化后位置跑掉。

动态壁纸、桌面增强软件或某些铺满屏幕的窗口也可能被 Windows 判断成全屏。关掉“全屏时隐藏”后立刻恢复，通常就是这一类情况。

### 开启鼠标穿透后无法操作词岛

这是正常行为。鼠标事件已经交给词岛下面的窗口了。

可以从系统托盘关闭穿透，或者直接用“找回词岛”恢复。

## 歌词与时序

### 只有歌名，没有歌词

先确认 SPW 自己能显示当前歌曲歌词。词岛能拿到歌名，不代表 SPW 已经把歌词行传过来。

可以依次试：

1. 切到另一首歌再切回来；
2. 如果用了其他歌词插件，确认它已经完成歌词加载；
3. 重新播放或重新加载当前歌曲。

本插件不会主动联网搜歌词。需要自动获取逐字歌词时，可以搭配 [SPW-Lyrics](https://github.com/GaBoron/SPW-Lyrics)。

如果只在多个歌词插件同时启用时出问题，反馈时把插件组合一起写上。

### 歌名也不对，或者切歌后还停在上一首

先再切一次歌曲。如果仍然能复现，最好记录：

- 本地歌曲还是网络歌曲；
- SPW 自己显示的曲目信息是否正确；
- 是否只发生在插件刚启用后的第一首歌；
- 再切一次歌曲后会不会恢复。

这些信息比单独一张截图更有用。

### 逐字高亮整体偏早或偏晚

在 SPW 插件设置里调整“逐字高亮偏移 ms”：

- 正值：高亮提前；
- 负值：高亮延后。

这个设置只能修正整体偏差。不同歌曲需要完全不同的 offset，通常是歌词源自己的时间轴差异。

### 调整播放倍速后明显不同步

当前对非 1× 播放速度的适配还不完整，逐字动画和歌词时间轴可能明显跑掉。这个问题已经记录在 [Issue #15](https://github.com/GaBoron/Dynamic-Lyrics-Island-for-SPW/issues/15)。

不要靠不断改 offset 去补倍速问题；排查普通时序时先回到 1×。

### 实验性多行歌词异常

如果出现不该重叠的歌词、间奏保留错行，或者只有部分歌曲表现异常，先关闭“实验性多行歌词”看默认单行模式是否正常。

只有实验模式出问题时，反馈最好附上具体歌曲、歌词来源和能稳定复现的大致时间点。

## 封面、进度与频谱

### 看不到专辑封面

插件只读取本地能访问到的封面：

1. 音频内嵌封面；
2. 同目录 `cover.jpg` / `cover.png`；
3. 同目录 `folder.jpg` / `folder.png`。

网络歌曲、不可访问文件或本来就没封面的歌曲会显示占位图。

如果本地歌曲明明有封面，可以换另一首本地歌对比，并确认 SPW 当前播放路径确实指向那个文件。

### 开启封面取色后没变化

先看封面本身有没有成功读到。显示占位图时，歌词、背景和频谱都会继续用默认颜色。

另外，取色会避开近黑和近白区域，不是直接读封面中心像素，所以结果和肉眼认定的“主色”可能不完全一样。

### 进度条不能拖，或者总时长是 `--:--`

拖动进度需要可靠的歌曲总时长。网络歌曲、本地文件暂时不可读，或者某些格式无法可靠解析时长时，进度控制会受限。

这不会影响歌词、上一首、播放 / 暂停和下一首。

如果只有某一种本地音频格式一直读不到时长，提 Issue 时把格式写上。

### Windows 实时频谱不动

先确认当前两侧信息设置里确实有频谱、歌曲正在播放，而且没有开启低性能模式。

实时频谱还需要：

- Windows build 20348 或更高；
- 可用的 .NET Framework 4.x；
- 共享模式音频输出。

独占模式、ASIO 或其他绕过共享音频链路的输出方式可能捕获不到。先切到共享模式测试最省事。

捕获启动失败时，插件会退回模拟频谱，并在当前插件生命周期里提示一次原因。歌词不会因此停掉。

### 低性能模式下频谱不像真实音乐

正常。低性能模式会主动关掉实时音频捕获，改用轻量模拟频谱。

### Linux 没有实时频谱

Linux 当前没有实现 SPW 进程实时音频频谱，所以对应设置也不会出现。见 [兼容性与限制](compatibility.md#功能对照)。

## 菜单、设置与字体

### 快捷菜单里找不到某个设置

托盘 / 右键菜单只放高频项。最大宽度、背景不透明度、圆角、字体、封面取色、背景进度等设置继续放在 SPW 原生插件配置页里。

菜单里没有，不代表功能被删了。

### 字体异常、缺字或自定义字体效果不对

默认歌词用插件内置 Noto Sans SC，不需要额外安装字体。

Windows 通过“歌词字体与字重”选择器设置；Linux 仍填写已安装字体名称。自定义字体后：

- 只影响歌词；
- 找不到该字体时会退回内置字体；
- 某个字符缺失时先用内置字体补字，韩文等仍缺失的字符继续使用系统字体；
- 会优先使用该字体已安装的真实字重，缺少所选档位时使用最接近的已有字重。

极少数字符连系统字体也没有时，仍可能出现缺字框。

## 性能

### 词岛卡顿或动画不流畅

先开“低性能模式”试一下。它会降低常规刷新率，简化逐字高亮和部分动效；Windows 下还会停掉实时音频捕获。

如果低性能模式下仍然明显卡顿，反馈时最好说明：

- Windows 还是 Linux；
- 系统、SPW 和插件版本；
- 是否开启实验性多行歌词；
- 是一直卡，还是只在展开、拖动、切歌或逐字歌词时卡。

“整个 SPW 都卡”和“只有词岛动画掉帧”不是同一类问题，尽量分清楚。

## Linux

Linux 目前仍是实验性支持，完整差异见 [兼容性与限制](compatibility.md#linux-实验性支持)。

### 词岛上右键没有菜单

当前 Linux 快捷设置入口在系统托盘，词岛区域右键暂不提供菜单。也可以直接用 SPW 原生插件设置页。

### 看不到托盘图标

插件优先使用 AppIndicator / Ayatana AppIndicator，不可用时再尝试 GTK StatusIcon。部分桌面环境默认没有可见托盘区域，或者需要额外启用 AppIndicator 支持。

托盘不可用时，词岛本身仍可能正常工作，设置可以继续从 SPW 里改。

### 拖动松手后短暂闪回原位置

这是当前已知问题。拖动结束时可能先闪回旧位置，再回到新位置；最终位置能正确保存时一般不影响使用。

如果最后保存的位置也错了，再单独提 Issue。

### Windows 专属设置在 Linux 中消失

正常。Linux 当前不会显示实时频谱、频谱取色、鼠标穿透、悬停自动隐藏和全屏时隐藏。这些 Windows 配置值会保留，切回 Windows 包后仍可继续用。

## 提交 Issue

上面的办法都没解决，可以到 [Issues](https://github.com/GaBoron/Dynamic-Lyrics-Island-for-SPW/issues) 反馈。

至少写清楚平台、系统版本、SPW 版本、插件版本、复现步骤，以及问题是每次都出现还是偶发。截图或录屏尽量同时带上 SPW 和词岛状态。

不同问题再补这些信息会更好：

- 歌词：歌曲、歌词来源、出问题的大致时间点；
- 频谱：音频输出方式，是否使用 ASIO / 独占模式；
- 封面：本地还是网络歌曲，封面是内嵌还是同目录文件；
- 多显示器：显示器数量、缩放比例、主屏；
- Linux：桌面环境，以及 X11 / Wayland / XWayland。

尽量把“期望发生什么”和“实际发生什么”都写出来，比只说“不能用”更容易定位。
